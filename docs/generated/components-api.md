<!-- AUTO-GENERATED by tools/docs.mjs — do not edit by hand. Run: npm run docs:generate -->

# 组件 API（自动生成）

共 40 个组件。属性 / 事件 / 插槽由 `.ets` 源码解析生成，权威用法见 [10 Components](../10-components.md)。

### AppAccordion

折叠面板：标题行点击展开/收起，内容通过 content 插槽注入。 展开状态由 expanded 双向持有；箭头随状态旋转。

**Props**

| 属性 | 类型 | 默认值 |
| --- | --- | --- |
| `title` | `ResourceStr` | `''` |
| `expanded` | `boolean` | `false` |

**事件**

| 回调 | 签名 |
| --- | --- |
| `onToggle` | `(expanded: boolean) => void` |

**插槽（@BuilderParam）**

| 名称 | 类型 |
| --- | --- |
| `content` | `() => void` |

### AppAvatar

头像：无图时用首字母 + 语义底色。尺寸建议 24/32/40/48/64。

**Props**

| 属性 | 类型 | 默认值 |
| --- | --- | --- |
| `text` | `string` | `''` |
| `avatarSize` | `number` | `Token.size.avatarMd` |
| `shape` | `AppAvatarShape` | `AppAvatarShape.Circle` |
| `bgColor` | `ResourceColor` | `Token.color.primary()` |

**枚举**

- `AppAvatarShape`: `Circle` · `Rounded`

### AppBadge

徽标：dot=true 显示 8vp 圆点；否则显示 count 数字（>99 显示 99+）。

**Props**

| 属性 | 类型 | 默认值 |
| --- | --- | --- |
| `count` | `number` | `0` |
| `dot` | `boolean` | `false` |

### AppBreadcrumb

面包屑：以分隔符连接的层级路径，最后一项为当前页（不可点）。 分隔符方向随 RTL 镜像。

**Props**

| 属性 | 类型 | 默认值 |
| --- | --- | --- |
| `items` | `ResourceStr[]` | `[]` |

**事件**

| 回调 | 签名 |
| --- | --- |
| `onSelect` | `(index: number) => void` |

### AppButton

按钮：Primary / Secondary / Text / Danger 四型，Large / Medium / Small 三档。 支持 loading（显示指示器并禁用点击）与 Pressed 缩放动画（0.98, Instant）。

**Props**

| 属性 | 类型 | 默认值 |
| --- | --- | --- |
| `label` | `ResourceStr` | `''` |
| `type` | `AppButtonType` | `AppButtonType.Primary` |
| `btnSize` | `AppButtonSize` | `AppButtonSize.Medium` |
| `disabled` | `boolean` | `false` |
| `loading` | `boolean` | `false` |

**事件**

| 回调 | 签名 |
| --- | --- |
| `onClickAction` | `() => void` |

**枚举**

- `AppButtonType`: `Primary` · `Secondary` · `Text` · `Danger`
- `AppButtonSize`: `Large` · `Medium` · `Small`

### AppCalendar

日历选择：胶囊入口点开日历面板，选中填充 Primary。

**Props**

| 属性 | 类型 | 默认值 |
| --- | --- | --- |
| `selected` | `Date` | `new Date()` |

**事件**

| 回调 | 签名 |
| --- | --- |
| `onPick` | `(value: Date) => void` |

### AppCard

通用卡片：Surface 底色 + Level1 阴影 + RadiusMD 圆角 + MD 内边距。 clickable=true 时整卡可点，Pressed 升至 Level2。

**Props**

| 属性 | 类型 | 默认值 |
| --- | --- | --- |
| `clickable` | `boolean` | `false` |

**事件**

| 回调 | 签名 |
| --- | --- |
| `onCardClick` | `() => void` |

**插槽（@BuilderParam）**

| 名称 | 类型 |
| --- | --- |
| `content` | `() => void` |

### AppCheckbox

复选框：左侧 Checkbox + 右侧文案。命中区 ≥ 44。

**Props**

| 属性 | 类型 | 默认值 |
| --- | --- | --- |
| `label` | `ResourceStr` | `''` |
| `checked` | `boolean` | `false` |
| `boxDisabled` | `boolean` | `false` |

**事件**

| 回调 | 签名 |
| --- | --- |
| `onCheck` | `(value: boolean) => void` |

### AppChipGroup

可删除标签组：自动换行排列，每个 chip 带关闭按钮。 删除动作回调 onDelete(index)，由业务更新 chips。

**Props**

| 属性 | 类型 | 默认值 |
| --- | --- | --- |
| `chips` | `ResourceStr[]` | `[]` |
| `chipDisabled` | `boolean` | `false` |

**事件**

| 回调 | 签名 |
| --- | --- |
| `onDelete` | `(index: number) => void` |

### AppDatePicker

日期选择器：内嵌滚轮式，选中项 Primary。

**Props**

| 属性 | 类型 | 默认值 |
| --- | --- | --- |
| `selected` | `Date` | `new Date()` |

**事件**

| 回调 | 签名 |
| --- | --- |
| `onPick` | `(value: Date) => void` |

### AppDialog

通用对话框：标题 + 正文 + 操作区（取消 / 确认）。 用法：new CustomDialogController({ builder: AppDialog({ ... }) })

**Props**

| 属性 | 类型 | 默认值 |
| --- | --- | --- |
| `title` | `ResourceStr` | `''` |
| `message` | `ResourceStr` | `''` |
| `confirmLabel` | `ResourceStr` | `$r('app.string.ds_dialog_confirm')` |
| `cancelLabel` | `ResourceStr` | `$r('app.string.ds_dialog_cancel')` |

### AppDivider

分割线：水平（可带居中文字）或垂直。 - 无 text：一条 divider 色细线。 - 有 text：线—文字—线。 - vertical=true：垂直细线（需由父容器给定高度）。

**Props**

| 属性 | 类型 | 默认值 |
| --- | --- | --- |
| `text` | `ResourceStr` | `''` |
| `vertical` | `boolean` | `false` |

### AppFormItem

表单项容器：统一 Label（可标必填）+ 控件插槽 + Error/Helper。 校验逻辑由业务持有；本组件只负责一致的标签与错误态呈现。 用法：把任意输入控件放进 content 插槽。

**Props**

| 属性 | 类型 | 默认值 |
| --- | --- | --- |
| `label` | `ResourceStr` | `''` |
| `required` | `boolean` | `false` |
| `helper` | `ResourceStr` | `''` |
| `error` | `ResourceStr` | `''` |

**插槽（@BuilderParam）**

| 名称 | 类型 |
| --- | --- |
| `content` | `() => void` |

### AppListItem

列表项：左图标（可选）+ 主/次标题 + 右附件文本（可选）+ 箭头（可选）。 单行 ≥ 56。新代码使用 leadingIcon + showLeadingIcon 渲染系统 Symbol。

**Props**

| 属性 | 类型 | 默认值 |
| --- | --- | --- |
| `title` | `ResourceStr` | `''` |
| `subtitle` | `ResourceStr` | `''` |
| `leadingIcon` | `Resource` | `$r('sys.symbol.square_grid_2x2')` |
| `showLeadingIcon` | `boolean` | `false` |
| `leadingGlyph` | `string` | `''` |
| `trailingText` | `ResourceStr` | `''` |
| `showArrow` | `boolean` | `false` |
| `showDivider` | `boolean` | `true` |

**事件**

| 回调 | 签名 |
| --- | --- |
| `onItemClick` | `() => void` |

### AppMenu

菜单面板：RadiusMD + Level3 阴影，项高 40。 用于 .bindMenu / .bindPopup 的自定义 builder，或直接内嵌浮层。

**Props**

| 属性 | 类型 | 默认值 |
| --- | --- | --- |
| `items` | `AppMenuItem[]` | `[]` |

### AppNavBar

顶部导航栏：返回按钮（可选）+ 标题 + 尾部操作插槽（可选）。 返回箭头方向随 RTL 镜像；命中区 ≥ 44。

**Props**

| 属性 | 类型 | 默认值 |
| --- | --- | --- |
| `title` | `ResourceStr` | `''` |
| `showBack` | `boolean` | `true` |

**事件**

| 回调 | 签名 |
| --- | --- |
| `onBack` | `() => void` |

**插槽（@BuilderParam）**

| 名称 | 类型 |
| --- | --- |
| `trailing` | `() => void` |

### AppPagination

分页器：上一页 + 页码窗口（最多显示 maxButtons 个）+ 下一页。 current 为 1 基页码；pageCount 为总页数。

**Props**

| 属性 | 类型 | 默认值 |
| --- | --- | --- |
| `current` | `number` | `1` |
| `pageCount` | `number` | `1` |
| `maxButtons` | `number` | `5` |

**事件**

| 回调 | 签名 |
| --- | --- |
| `onChange` | `(page: number) => void` |

### AppPopover

气泡浮层内容容器：Surface 底 + RadiusMD + Level3 阴影，最大宽 240。 用于 .bindPopup 的自定义 builder（CustomPopupOptions.builder）。

**Props**

| 属性 | 类型 | 默认值 |
| --- | --- | --- |
| `title` | `ResourceStr` | `''` |
| `text` | `ResourceStr` | `''` |

### AppProgress

进度：线性（高 4）或环形（56）。前景 Primary，底 Outline。

**Props**

| 属性 | 类型 | 默认值 |
| --- | --- | --- |
| `value` | `number` | `0` |
| `total` | `number` | `100` |
| `kind` | `AppProgressKind` | `AppProgressKind.Linear` |

**枚举**

- `AppProgressKind`: `Linear` · `Ring`

### AppRadioGroup

单选组：横向或纵向排列，选中项高亮 Primary。命中区 ≥ 44。

**Props**

| 属性 | 类型 | 默认值 |
| --- | --- | --- |
| `options` | `AppRadioOption[]` | `[]` |
| `selected` | `string` | `''` |
| `groupName` | `string` | `'app-radio-group'` |
| `vertical` | `boolean` | `false` |

**事件**

| 回调 | 签名 |
| --- | --- |
| `onSelect` | `(value: string) => void` |

### AppRating

星级评分：max 颗星，rating 为已选数。ratingReadonly=true 仅展示不可点。

**Props**

| 属性 | 类型 | 默认值 |
| --- | --- | --- |
| `rating` | `number` | `0` |
| `max` | `number` | `5` |
| `ratingReadonly` | `boolean` | `false` |

**事件**

| 回调 | 签名 |
| --- | --- |
| `onRate` | `(value: number) => void` |

### AppSearch

搜索框：胶囊外形（RadiusFull），左侧搜索图标，有内容时右侧可清除。

**Props**

| 属性 | 类型 | 默认值 |
| --- | --- | --- |
| `text` | `string` | `''` |
| `placeholder` | `ResourceStr` | `$r('app.string.ds_search_placeholder')` |

**事件**

| 回调 | 签名 |
| --- | --- |
| `onTextChange` | `(value: string) => void` |
| `onSubmit` | `(value: string) => void` |

### AppSegmented

分段控制器：胶囊底槽 + 选中态浮起白片。 与 AppTabs 的区别：Segmented 用于同级视图切换的紧凑开关，非页面级导航。

**Props**

| 属性 | 类型 | 默认值 |
| --- | --- | --- |
| `segments` | `ResourceStr[]` | `[]` |
| `selectedIndex` | `number` | `0` |

**事件**

| 回调 | 签名 |
| --- | --- |
| `onSelect` | `(index: number) => void` |

### AppSelect

下拉选择：封装系统 Select，统一按 Token 主题化（字号/字色/圆角/选中色）。 单选。options 为可读文案数组；selectedIndex 为当前选中项（-1 表示未选）。

**Props**

| 属性 | 类型 | 默认值 |
| --- | --- | --- |
| `options` | `ResourceStr[]` | `[]` |
| `selectedIndex` | `number` | `-1` |
| `placeholder` | `ResourceStr` | `''` |
| `selectDisabled` | `boolean` | `false` |

**事件**

| 回调 | 签名 |
| --- | --- |
| `onSelectIndex` | `(index: number) => void` |

### AppSheet

底部面板内容容器：顶部拖拽条 + 可选标题 + 自定义内容。 配合调用方的 .bindSheet($$show, AppSheet(){...}) 使用。

**Props**

| 属性 | 类型 | 默认值 |
| --- | --- | --- |
| `title` | `ResourceStr` | `''` |

**插槽（@BuilderParam）**

| 名称 | 类型 |
| --- | --- |
| `content` | `() => void` |

### AppSlider

滑块：已选段 Primary，未选 Outline。可选右侧数值显示。

**Props**

| 属性 | 类型 | 默认值 |
| --- | --- | --- |
| `value` | `number` | `0` |
| `min` | `number` | `0` |
| `max` | `number` | `100` |
| `step` | `number` | `1` |
| `showValue` | `boolean` | `false` |

**事件**

| 回调 | 签名 |
| --- | --- |
| `onValueChange` | `(value: number) => void` |

### AppStepper

数字步进器：[-] 值 [+]。受 min/max/step 约束，越界时对应按钮禁用。

**Props**

| 属性 | 类型 | 默认值 |
| --- | --- | --- |
| `value` | `number` | `0` |
| `min` | `number` | `0` |
| `max` | `number` | `99` |
| `step` | `number` | `1` |
| `stepperDisabled` | `boolean` | `false` |

**事件**

| 回调 | 签名 |
| --- | --- |
| `onChange` | `(value: number) => void` |

### AppSteps

步骤条（水平）：序号圆点 + 连接线 + 标签。 current 为 0 基当前步；已完成/当前用 Primary，未达用 outline。

**Props**

| 属性 | 类型 | 默认值 |
| --- | --- | --- |
| `steps` | `ResourceStr[]` | `[]` |
| `current` | `number` | `0` |

### AppSwiper

轮播容器：封装系统 Swiper，指示点按 Token 主题化。 每页内容放入 pages 插槽（多个直接子组件按页轮播）。

**Props**

| 属性 | 类型 | 默认值 |
| --- | --- | --- |
| `autoPlay` | `boolean` | `false` |
| `interval` | `number` | `3000` |
| `loop` | `boolean` | `true` |

**插槽（@BuilderParam）**

| 名称 | 类型 |
| --- | --- |
| `pages` | `() => void` |

### AppSwitch

开关：可选左侧文案 + 右侧 Switch。命中区 ≥ 44。

**Props**

| 属性 | 类型 | 默认值 |
| --- | --- | --- |
| `label` | `ResourceStr` | `''` |
| `isOn` | `boolean` | `false` |
| `switchDisabled` | `boolean` | `false` |

**事件**

| 回调 | 签名 |
| --- | --- |
| `onToggle` | `(value: boolean) => void` |

### AppTable

轻量表格：表头 + 数据行，随 compact 收紧行高/字号。非虚拟滚动，适合中小数据量。 rows 为二维文案数组，列数应与 columns 对齐。

**Props**

| 属性 | 类型 | 默认值 |
| --- | --- | --- |
| `columns` | `AppTableColumn[]` | `[]` |
| `rows` | `ResourceStr[][]` | `[]` |

### AppTabs

分段标签栏：等宽标签，选中项字色 Primary + 2vp 底部指示条。 仅负责标签切换；内容区由调用方根据 selectedIndex 渲染。

**Props**

| 属性 | 类型 | 默认值 |
| --- | --- | --- |
| `tabs` | `ResourceStr[]` | `[]` |
| `selectedIndex` | `number` | `0` |

**事件**

| 回调 | 签名 |
| --- | --- |
| `onSelect` | `(index: number) => void` |

### AppTag

胶囊标签，颜色使用状态语义 Token。

**Props**

| 属性 | 类型 | 默认值 |
| --- | --- | --- |
| `label` | `ResourceStr` | `''` |
| `tone` | `AppTagTone` | `AppTagTone.Neutral` |

**枚举**

- `AppTagTone`: `Neutral` · `Primary` · `Success` · `Warning` · `Danger` · `Info`

### AppTextField

输入框：Label（上）+ 输入区 + Helper/Error（下）。 状态：Default(Outline) / Focused(Primary 2vp) / Error(Danger) / Disabled(Divider + 38%)。

**Props**

| 属性 | 类型 | 默认值 |
| --- | --- | --- |
| `label` | `ResourceStr` | `''` |
| `text` | `string` | `''` |
| `placeholder` | `ResourceStr` | `''` |
| `helper` | `ResourceStr` | `''` |
| `error` | `ResourceStr` | `''` |
| `fieldSize` | `AppTextFieldSize` | `AppTextFieldSize.Medium` |
| `fieldFontFamily` | `string` | `Token.font.family` |
| `fieldDisabled` | `boolean` | `false` |

**事件**

| 回调 | 签名 |
| --- | --- |
| `onTextChange` | `(value: string) => void` |

**枚举**

- `AppTextFieldSize`: `Medium` · `Small`

### AppTimePicker

时间选择器。 - Field（默认）：可点字段，展示已选时间 + 时钟图标，点击弹出 TimePickerDialog；支持 label/helper/error/disabled。 - Inline：常驻滚轮，选中项 Primary。 支持 compact 密度；默认 24 小时制，可选显示秒。

**Props**

| 属性 | 类型 | 默认值 |
| --- | --- | --- |
| `mode` | `AppTimePickerMode` | `AppTimePickerMode.Field` |
| `selected` | `Date` | `new Date()` |
| `label` | `ResourceStr` | `''` |
| `placeholder` | `ResourceStr` | `$r('app.string.ds_time_placeholder')` |
| `helper` | `ResourceStr` | `''` |
| `error` | `ResourceStr` | `''` |
| `fieldDisabled` | `boolean` | `false` |
| `useMilitaryTime` | `boolean` | `true` |
| `showSeconds` | `boolean` | `false` |

**事件**

| 回调 | 签名 |
| --- | --- |
| `onPick` | `(value: TimePickerResult) => void` |

### AppTooltip

文字提示：点击触发元素弹出简短说明气泡（移动端以点按代替悬停）。 触发元素放入 content 插槽；message 为提示文案。

**Props**

| 属性 | 类型 | 默认值 |
| --- | --- | --- |
| `message` | `ResourceStr` | `''` |
| `placement` | `Placement` | `Placement.Top` |

**插槽（@BuilderParam）**

| 名称 | 类型 |
| --- | --- |
| `content` | `() => void` |

### AppTree

树形控件：可展开的层级列表。展开态存于节点自身；点击父节点切换。 通过内部 revision 触发重绘（节点展开态为普通对象属性）。

**Props**

| 属性 | 类型 | 默认值 |
| --- | --- | --- |
| `nodes` | `AppTreeNode[]` | `[]` |

**事件**

| 回调 | 签名 |
| --- | --- |
| `onSelect` | `(node: AppTreeNode) => void` |

### AppUpload

上传占位控件：虚线「添加」区 + 已选文件列表（可删除）。 不内置文件选择器；由业务在 onAdd 中调用系统 Picker，并回填 files。

**Props**

| 属性 | 类型 | 默认值 |
| --- | --- | --- |
| `files` | `ResourceStr[]` | `[]` |
| `hint` | `ResourceStr` | `$r('app.string.ds_upload_hint')` |
| `uploadDisabled` | `boolean` | `false` |

**事件**

| 回调 | 签名 |
| --- | --- |
| `onAdd` | `() => void` |
| `onRemove` | `(index: number) => void` |

### Skeleton

骨架屏占位块：Divider 底色 + 循环微光动画。

**Props**

| 属性 | 类型 | 默认值 |
| --- | --- | --- |
| `blockWidth` | `Length` | `'100%'` |
| `blockHeight` | `Length` | `Token.size.skeletonLine` |
| `radius` | `number` | `Token.radius.xs` |

### StateView

状态页：Loading / Empty / Error 统一结构（图标 + 标题 + 说明 + 可选行动）。

**Props**

| 属性 | 类型 | 默认值 |
| --- | --- | --- |
| `kind` | `StateKind` | `StateKind.Empty` |
| `title` | `ResourceStr` | `''` |
| `description` | `ResourceStr` | `''` |
| `actionLabel` | `ResourceStr` | `''` |

**事件**

| 回调 | 签名 |
| --- | --- |
| `onAction` | `() => void` |

**枚举**

- `StateKind`: `Loading` · `Empty` · `Error`
