# ── OS ─────────────────────────────────────────────
.DS_Store
**/.DS_Store
Thumbs.db

# ── Logs ───────────────────────────────────────────
*.log
**/*.log

# ── HarmonyOS / DevEco 构建与缓存产物（任意模块）──────
# 编译输出
**/build/
# hvigor 缓存（注意：仓库里的 hvigor/ 包装器目录需要保留，勿删）
**/.hvigor/
# Native 构建
**/.cxx/
# 语言服务缓存
**/.clangd
**/.clang-format

# ── 依赖目录 ─────────────────────────────────────────
**/node_modules/
**/oh_modules/

# ── 机器相关 / 敏感配置 ───────────────────────────────
# 本地 SDK 路径等，因机器而异，不应入库
**/local.properties
# 签名材料
*.p12
*.cer
*.p7b
